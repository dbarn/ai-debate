{
  "name": "ai-debate-webapp",
  "version": "1.0.0",
  "description": "Two-AI debate web app with selectable providers and prompt modifiers (dark mode, 3px radius).",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "open-cli http://localhost:3000",
    "dev": "node server.js & open-cli http://localhost:3000"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "helmet": "^7.1.0",
    "node-fetch": "^3.3.2"
  },
  "devDependencies": {
    "open-cli": "^8.0.0"
  }
}
